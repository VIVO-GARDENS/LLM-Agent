# Credenciales — nunca, bajo ninguna circunstancia
.env
.env.*
!.env.example
*.key
*.pem

# Memoria de precios: contiene cotizaciones del negocio y contexto de
# conversaciones con clientes. Se siembra sola desde agente/precios.py.
memoria/

# Resultados de corridas de evaluación (traen transcripciones)
*.json
!.github/**/*.json
# El workflow de n8n SI se versiona: es el despliegue, y no lleva secretos
# (las credenciales viven dentro de n8n y aqui solo se referencian por id).
!n8n/workflow.json
corrida*.json

# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/

# Herramientas
.playwright-mcp/
.pytest_cache/
.vscode/
.idea/

# Sistema
Thumbs.db
.DS_Store
